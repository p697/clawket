# 24 · Mobile task workflows

Owner authorization, 2026-09-21: implement the product-research recommendations, including the clarified skill picker, completed-conversation scheduling, memory entry, iOS/Android system sharing and home-screen widgets; include previously proposed 3.1 work. Validate on the connected Android device and iOS simulators against both local backends. Prefer concise controls to explanatory copy. This authorization supersedes earlier exclusions where explicitly covered; compatibility, privacy, lifecycle and resource limits remain mandatory.

## Delivery ledger

| Milestone | Scope | Status |
|---|---|---|
| W1 | Available installed skills in chat; scoped draft selection; real backend invocation | In progress |
| W2 | Completed conversation to a standalone scheduled prompt; existing Cron editor and confirmation; memory/about-me entry | In progress |
| W3 | Native incoming text/URL/image/file sharing, destination choice, retained draft and explicit send | In progress |
| W4 | Native iOS and Android home-screen widgets; adaptive monochrome presentation; authenticated in-app shortcuts | In progress |
| W5 | Hermes native skill installation/status, documents, provider health/configuration | In progress |
| W6 | Negotiated current-run steering, Hermes approval/clarification, generic remote task notifications | In progress |
| W7 | Result handling, search/archive/export, session organization, contextual paywall/value-first experience | In progress |
| W8 | Complete required/compatibility gates, native builds, dual-backend device journeys, screenshot review, payment regression | In progress |

## Remaining implementation and release boundaries

This is the current consolidated remainder; historical evidence below is chronological, not a list of current failures.

- Native clarification: the installed Hermes run API exposes approval and steer, but no clarification event/answer endpoint. A Clawket-owned compatibility implementation and cancellation/recovery tests are still needed; external Hermes source remains read-only.
- Remote completion/attention delivery: APNs/FCM registration, revocation, deduplication and server delivery are not implemented. Credentials and signed-device acceptance are separate human tasks, not substitutes for the missing implementation.
- Generated result files: ordinary document input and conversation export are implemented; generated-file cards with authenticated, scoped retrieval/open/save are not. The native browser-control artifact API is not a general host filesystem API.
- Advanced Hermes model/provider configuration and budget insights remain incomplete. Archive naming, pinning, pinned/file filters and stable sorting are now implemented over immutable local snapshots; basic organization remains free, with contextual Pro search/bulk export. Basic global model selection/health and single export already exist.
- Conversation scheduling currently builds a bounded, reviewable brief with ordered user requirements and the selected prior result. It does not make a separate AI summarization request. Reply continuation seeds only the selected response, not a full transcript fork.
- A new universal seven-day Pro trial is not implemented: no fabricated local expiry. Existing verified entitlements/grace remain; store offer eligibility and commercial terms still need a supported source.
- Android legacy draft recovery and iPad simulator Hermes chat/landscape acceptance are now complete for Release 527. Acceptance remainder includes iPad physical-device/window/keyboard cases, store-signed billing/upgrade and broader interruption/network cases. QA Pro override is not billing proof. Production Relay/Registry and store release have not occurred.

## UX and correctness

- Skills: Add → Use skill → searchable installed/available list → draft reference. Selection never sends. Unavailable skills cannot be invoked; selection belongs to the mounted connection/Agent. Backend invocation is resolved by adapters, not screen backend branches.
- Scheduling: a completed conversation contributes a reviewable standalone task containing the user's final requirements. Opening the editor never creates a job. Preserve existing draft-to-Cron action. A prior scheduled-task tool result must not silently create a duplicate.
- Memory: direct entries into the existing document reader; file rows and document titles retain original filenames (including MEMORY.md and USER.md), without localized aliases. Do not introduce a second editor or claim MEMORY.md contains all backend memory.
- Sharing: copy temporary OS payloads into owned storage, bound count/size/type, retain until a user submission receives a backend acknowledgement, and ask for an eligible destination. Never auto-send or overwrite a current draft. Sharing remains usable through onboarding and cold start.
- Widgets: no message bodies, tokens, live connection claims or background sockets. Launch into the normal navigation, capability and entitlement flow. Owner update 2026-09-22: the voice widget tap starts the existing recording flow directly after foreground navigation, access/capability checks and scoped draft restoration. No extra confirmation sheet; preserve system permission and saved-recording recovery handling.
- Notifications: opt-in, generic metadata only, revocable registrations, deduplicated outcomes, verified backend completion. Local notifications are not a substitute for APNs/FCM delivery while terminated. Missing credentials/environment capabilities remain explicit delivery blockers.
- Paid features: preserve purchased/lifetime rights, store-localized prices and current verified entitlements. Trials may not be fabricated from device timestamps or inferred from a SKU. Do not make real charged purchases during automated QA.

## Acceptance

Record build identity, device/platform, backend/service environment, test scope and evidence for each journey. Test both themes, long labels, large text, cancelled operations, stale responses, cold starts and connection changes. Native signing/entitlement and actual store-delivered billing validation are distinct from compile/unit-test success. Production changes require the established release compatibility and Registry recovery gates. No incomplete milestone may be reported as production-ready.

## Working evidence (not release acceptance)

- 14:45: Release 527 built/installed on both phones and iPad simulator. Android actual archive rename/pin (536), file filter (540), native export (538), legacy preview → explicit restore → cold persistence → clear → empty cold restart (550–560) verified; no fixture was sent. iOS exact rename and immutable source-title readback passed 555/560; cold restart and pinned filter passed 562. Earlier 547 appended text and is not a pass. iPad native Photos pairing with the correct /v1/hermes/ws QR and actual new-session send/reply passed 549; dark-landscape chat and Session Panel captures reviewed. Earlier wrong-path QR and moving-keyboard tap failures are not counted. Latest complete gates remain required 526 / v1 531 / native Hermes 533; no later production-source edits. Summary: [acceptance-workflows-2026-09-21.md](acceptance-workflows-2026-09-21.md).

- 13:47: required 508 passed (322 Mobile suites / 3,318 tests; Bridge 36 files / 316 tests), dual-platform Release 508 built/installed. Android Discover now opens without the native crash; actual catalog → search → source-qualified Weather install → installed list → source reader → confirmed uninstall was operated through ADB/UI captures 511–516. Native directory, Hub registry and usage records are absent after cleanup. iOS native install/uninstall passed 490/495, reinstall source verified 498 (496 incorrectly appended its search query twice and is not a complete pass), and updated catalog passed 509. Android Maestro 497 lost USB at launch, so its replacement direct-device evidence is explicitly separate. Last Bridge-native integration 492 passed 37 and v1 494 passed 39, with no later Bridge changes. Phone returned to the widget page; normal font/light appearance. Wider implementation/release remainder above is still open.

- 13:39 candidate: Android 505 opening Discover crashed in `RNCWebViewManagerDelegate` while casting the string decelerationRate to Double. The iOS wrapper normalizes this convenience value; Android forwards it unchanged. Regression 507 reproduced the prop-type mismatch; the candidate uses numeric 0.998, preserving the iOS normal rate. Fresh native builds, full gate and Android catalog/install acceptance are required. Earlier gate 494 was green but does not certify this later fix or the previously untested Android WebView.

- 13:27 candidate: actual iOS 490 ClawHub Weather install → refreshed installed list → native source read passed. Review exposed generic managed provenance and inspection found Hub uninstall could leave its install record behind. Native integration 491 reproduced the provenance gap; the candidate now resolves contained registry paths, reports ClawHub, and uses native Hub removal while retaining manual-skill behavior. Self-contained 492 passed 36 files / 316 tests, including refusal/corrupt/escape/ambiguous registry regressions. Full gate, new native builds and actual uninstall/reinstall acceptance are pending; QA Weather is still installed temporarily.

- 13:19: required 483 passed (322 Mobile suites / 3,318 tests), dual-platform Release 483 built. iOS 481/482 legacy draft preview → explicit restore → cold persistence → clear/no-recovery passed; both QA keys were removed. The preview title/button now say Recover draft/Restore instead of Edit message/purchase wording. iOS paywall 484 opened/closed three times, retained navigation, and restored the simulated-free override OFF. Screenshots reviewed; these are presentation checks, not signed billing or purchases.

- 13:05: required 465 passed (322 Mobile suites / 3,318 tests), dual-platform Release 465 built/installed. Permanent approval requires explicit confirmation scoped to the still-pending ID and advertised decisions, with OpenClaw/Hermes cancellation/replacement/resolution regressions. iOS 468 long-press → cancel → pending → deny passed whole flow. Android 476 confirmation was directly operated/captured; its native 300-second timeout then refused the command and retired the modal/card. This is not an Android cancellation-flow pass; 469 lost the automation driver at launch. Both temporary native configurations were restored byte-identically and owned fixtures removed. No permanent permission was granted.

- 12:48: real native Hermes manual approval exercised on both platforms. iOS 448/450 pending → allow-once → QA_ALLOWED passed whole flows; only the owned target was removed. Android 455 pending assertions/screenshot and 459 denied-result assertion passed, but USB failed at teardown/final capture; separate ADB 462 shows ANDROID_QA_DENIED and the owned target remained byte-identical. Authenticated Bridge 463 confirms both runs idle with zero pending approvals. Native config was restored byte-identically by the guarded QA process, private backup removed, fixtures cleaned. Source follow-up now requires a separate confirmation for permanent approval rather than granting on long press; native build/verification pending.

- 12:27: required 441 passed (322 Mobile suites / 3,316 tests), v1 435 passed 39, external Hermes integration 434 passed 36. Initial integration 430 lacked the explicit native Python environment; 431 exposed an obsolete prewarm assertion counting capability probes as model reads. It now requires exactly one catalog read and no additional Python work for the following model request. Real iOS Skill restore first exposed trimmed source content; dispatch/read now preserve exact whitespace, native integration checks exact on-disk/readback equality, and fresh iOS 432/434 restored all 133 bytes including final newlines. Android 438 search → keyboard-dismissed detail → source passed whole flow; 436 lost USB and is not a pass. Dual-platform Release 441 built/installed. iOS 442 source formatting and visible native version-deletion confirmation passed; temporary QA skills are being cleaned. Later 3.1 scope remains incomplete; production unchanged.

- 12:07 follow-up: actual ClawHub weather download exposed the installed Hermes source dropping owner identity before HTTP resolution. The Clawket subprocess now adds ownerHandle only to the requested resource/download, rejects absent/mismatched owner metadata, preserves native quarantine/scanning, refuses existing manual directories and Hub records, serializes installations, and verifies the resulting directory. Real native install in an isolated QA home passed, including readback, repeat rejection and unchanged content; the QA home was removed. Self-contained Bridge 35 files / 308 tests passed. Required 418 passed (Mobile 322 suites / 3,315 tests); later Skills detail keyboard dismissal has 26 targeted regressions and still requires final native checks. Oversized documents now show a concise version-history limitation only while editing. No external Hermes source or production service changed.

- First required-gate pass included Mobile 311 suites and the initial sharing/skill/scheduling work. Subsequent run-control changes require a fresh gate.
- Sharing/store/draft/Thread tests: 45 passed. Hermes Bridge self-contained tests after native Hub and exact-run control: 30 files / 246 passed.
- Android arm64 Release QA build/install passed on Samsung A56; native SEND text opened destination selection without sending. Full target/restore/file/widget/backend journeys remain pending.
- iOS clean prebuild and Pods passed after widget localization configuration fixes. Simulator Release build and install passed; no device signing or store distribution claim.
- Build caches created by this task were moved to `/Volumes/Lucy-SSD/Relocated/Caches/dev/clawket-workflows-0921/` after system-disk exhaustion. Android local QA uses `com.p697.clawket.qa` and retains the original store app.
- Native install and steering are candidates awaiting real backend journeys. OpenClaw exact-run steering stays unavailable because the inspected `chat.send queueMode=steer` can start a new turn when idle; it cannot meet exact-run semantics without a stronger negotiated contract.

- Follow-up: Android incoming text share → Hermes draft visually confirmed after the current QA rebuild, with no automatic send. Native notebook extraction passed against the installed Hermes converter. Candidate Bridge self-contained tests: 31 files / 252 tests; protocol branch coverage 100%; v1 compatibility 39 tests; fixed recovery release matrix four combinations × six phases passed. A fresh full gate is still required after later document-version changes.
- Production service versions were checked again and still match the 09-16 snapshots. Hermes `/v1/hermes/pair/claim-code` returns 404 in Production; existing QR/local paths remain the QA route. Production deployment remains the post-acceptance release step; no services were deployed. Fixed recovery artifacts: `evidence/workflows-2026-09-21/registry-recovery/`.
- Added candidate local version history for personal memory and SKILL.md, conflict checking before writes, free personal memory corrections and explicit review-before-restore. Manual sessions created in the App are persisted by exact connection/Agent/session scope and remain writable on an otherwise accessible free Agent. These are not yet complete device acceptance.

- iOS installed-skill journey passed against native Hermes: explicit `arxiv` selection → `skill_view` → `HERMES_SKILL_OK`. Visual review found a composer-clear prefix regression and a stale live tool alias; candidate fixes and focused regressions passed (9 suites / 162 tests), awaiting rebuilt-device confirmation. Search now preserves free access to main/App-created conversations with exact connection/Agent checks. Widget placement and appearance acceptance is underway.
- Android rebuilt skill invocation returned `ANDROID_SKILL_ACCEPTED` and cleared the selected skill. Tool-row convergence still needs final verification: terminal reads now wait for the prior tool-result read and bypass its cooldown. iOS visual inspection also found the focused composer keyboard obscuring Add; candidate blur and sheet-aware skill search fix is being rebuilt.
- Latest complete required gate passed with Mobile 314 suites / 3,223 tests. Later model-health, share-acknowledgement, history-refresh and document-owner changes have focused tests and require a fresh full gate. Native Hermes model-health import/read/probe succeeded, reporting DeepSeek reachable without exposing credentials. Configuration and manual probe UI remain under device acceptance.
- iOS small widget registration, Home Screen placement and chat entry were visually exercised. Medium/dark/Android widgets, native iOS incoming share and remaining run-control/payment journeys are not yet accepted. W6 clarification/remote delivery and W7 advanced result/export/trial scope remain unfinished.
- Follow-up iOS build 5 verified the skill draft clears, Add is usable with the keyboard initially open, and the completed native skill run converges to one tool record. Model-health configuration read and explicit DeepSeek probe passed. Memory edit → diff → restore → save passed with byte-identical original content restored; corrected a restore-purchases translation accidentally reused by the version button.
- Cron conversation import/create/readback/pause passed. Native execution exposed the legacy `none` delivery mapping and false-success output parser; corrected native `local` mapping, explicit skipped/blocked outcomes and unknown-format status. The test job `b8bd0dcc8413` is task-owned and must be paused/removed after the active rerun; full execution acceptance remains pending. Long imported prompts now use a compact preview and the shared prompt editor.
- Latest complete required pass: Mobile 315 suites / 3,236 tests, plus remaining repository gates. Subsequent approval-deadline and Cron fixes require a fresh gate. Approval deadlines are now nullable when absent upstream; exact native expiry responses retire only that request. This is still working evidence, not release sign-off.

- Hermes Cron executed the expected `CLAWKET_CRON_QA_OK` result after the local-delivery correction; the task-owned job `b8bd0dcc8413` was removed through the iOS editor. Native output reading is now bounded and path-confined, and the App leads with the response rather than embedded scheduler instructions.
- iOS build 7 verified new-session creation and native current-run steering (`IOS_STEER_VERIFIED`). The first attempt exposed a keyboard-obscured menu; composer blur fixed it. Follow-up fixes address optimistic steering echo identity and stable native history IDs. Native smart approval automatically accepted the disposable-file cleanup, so this is not yet a manual approval-card acceptance test.
- Added negotiated Hermes per-job model pins with catalog resolution and explicit clear-to-default behavior. Native stopping remains active until cancellation is confirmed; removed the unconditional five-second local clear and covered active/failed stops on both adapters. These later changes still need fresh native builds and complete gates.

- 05:50 follow-up: complete required pass (Mobile 316 suites / 3,252 tests). iOS Safari URL share survived a cold start and sent only after explicit input; native stop status confirmed cancelled, while the UI showed Hermes' Operation interrupted. Android widget placement and chat shortcut worked on Samsung A56. Later export UI tests and widget repeat-prebuild settings require fresh gates. Markdown/JSON export now captures complete stable pagination under the existing access checks; archive/bulk export remain unfinished.

- 06:46 follow-up: full required gate passed (Mobile 317 suites / 3,261 tests; Bridge 34 files / 295 tests), v1 39 passed, production-snapshot local rollout/recovery matrix four combinations × six phases passed. No production or Cloudflare migration deployment. Later native-share return behavior and widget paywall scope checks have focused tests and still need the final gate.
- iOS build 67 passed conversation long press → export → actual native share controller, verified by screenshot and a 40,767-byte Markdown export containing 113 message headings and current QA replies. This is export generation/native presentation evidence; a completed Files-app save was not confirmed. Fixed nested-sheet dismissal with push stacking for session actions; export now waits for the session panel and its own sheet to close before native presentation. Android build 67 installed APK SHA-256 matches the local candidate despite an interrupted install command; Maestro still loses the USB device during startup, so Android export UI is being checked directly.
- Shared-file retention is bounded at 100 MiB / 200 groups. Text-only input creates no directories. Explicit cache clearing serializes against captures/acknowledgements, retains all pending drafts and deletes consumed copies only after referenced caches/favorites clear successfully. Storage/maintenance regression: 9 passed. Mac lock prevents CUA desktop access; authorized simulator automation and screenshot files remain available.

- 07:18: Android source-built sharing module passed My Files TXT → native share → Clawket destination → Hermes attachment draft → explicit send → `DOCUMENT_INPUT_0921_OK`, with draft cleared. iOS Markdown export also completed Save to Files; the saved 41 KB document is visible in Files. Maestro text targeting failed on the out-of-process native save surface; screenshot-grounded taps completed the actual flow. The latest candidate removes the duplicate document label, with 82 Thread view tests passed.
- Native Hermes context restoration now positively detects the installed handler fallback and omits explicit history when native history exists; the old API stringified structured tool blocks. A bounded read-only repair projects the exact historical Bridge tool repr back into a tool without modifying SQLite. Added pending approval recovery from exact polled run status and resolved-request tombstones. Bridge self-contained suite: 35 files / 301 tests passed; local CLI rebuilt/restarted and health advertises the new capabilities. Later changes are undergoing a fresh required gate.

- 07:42 candidate: complete-export snapshots can be saved locally and opened from Search → Saved conversations. Reading and single export stay free; full-text archive search and bulk JSON export are Pro actions. Storage is bounded at 100 records / 1M total text characters / 500K per snapshot with no automatic eviction; deleting a saved copy never deletes the native session. Connection removal clears its owned copies. Document composer tiles now expose two filename lines. Service and UI regressions passed; rebuilt-device acceptance remains pending.

- 08:04: required 175 passed (Mobile 319 suites / 3,276 tests; Bridge 35 files / 301 tests; remaining workspace/design/i18n/docs gates). iOS Release 172 passed composer focus → sessions (keyboard dismissed) → long-press → save full transcript → Search → saved-copy list; screenshot reviewed. Actual OpenClaw skill invocation read the selected instructions and returned IOS_OC_SKILL_0921_OK. Native transcript proved a newly recovered introduction belonged before the tool/final; reconciliation now inserts it at confirmed canonical anchors, with a regression. Android Release rebuild and archive export/read acceptance continue.

- 08:21: isolated Cloudflare migration drill passed for both backends: legacy → candidate/v1 SQLite limiter → fixed recovery → recovery redeploy, saved pairing reconnect, new register/claim, controlled backend handshake/health/chat/sessions and retained 10/hour registration limit. Four dedicated Workers and two new KV namespaces were deleted after evidence capture. This is cloud control-plane/protocol evidence; production remains unchanged and local backend fixture execution is not mobile/LLM acceptance. See registry-recovery-runbook.md.
- Later mobile fixes: OpenClaw native nextOffset/hasMore history pagination now maps into session-bound App cursors; Hermes opaque cursors remain unchanged. 74 adapter/export regressions and Mobile typecheck passed; Release 192 rebuilt both platforms. Archive search/read/native single export and confirmed copy deletion passed iOS, with source conversation untouched. Shared sheet trailing actions now reserve their actual width instead of overlapping long titles. These edits still require the final complete gate.

- 08:25: Android Release 192 actual widget → OpenClaw installed-skill picker passed at night mode / 130% system font; light/dark widget screenshots reviewed. Isolated cloud resources and private QA pairing credentials cleaned; subsequent namespace listing confirms absence. Read-only production Registry checks still match original snapshot versions. Complete gate 221 passed (319 Mobile suites / 3,277 tests); later two canonical-order export/scheduling regressions passed and need the next final native/gate build.

- 08:36 candidate: share acknowledgement no longer depends on an empty current composer, so typing the next question before backend acknowledgement does not strand the prior sent share. Dismissed pending shares no longer reappear on each foreground/widget link; a new OS share and cold start remain recoverable. Seven targeted sharing regressions passed; actual observed Android pending-file origin is not yet proven, so this is not a claim that every reappearance was a duplicate.

- 08:48 candidate: reply continuation creates a new App-owned session with the selected completed reply in its editable draft, never a full-context clone or automatic send. Native Hermes fork inspection showed it ends the source session, so that endpoint is deliberately not invoked. Voice widget now prepares an explicit recording sheet while keeping the prior draft; Dynamic Type inspection led to bounded compact-header text and unscaled decorative avatar glyphs. These later edits require fresh gates/builds/device checks. Required 234 and dual-platform Release 235 passed before these edits; 235 installed on iOS and Android (device update time 07:43:42). iOS medium widget actual placement and small/medium dark accessibility-size screenshots reviewed.

- 08:56: required 246 passed (Mobile 322 suites / 3,293 tests; remaining required gates), Release 247 built/installed both platforms. Actual iOS voice-widget entry displayed explicit recording controls at accessibility-extra-large with no automatic capture; compact header/avatar no longer clipped. iOS completed OpenClaw reply → New session produced an unsent matching draft in a new conversation. Later RosterRow intrinsic-height fix has 25 component regressions and still needs the next build; no full release claim.

- 09:17: required 261 passed (Mobile 322 suites / 3,294 tests), dual-platform Release 259 built. Android 259 updated in place (device update 08:12:18); the observed cold-start duplicate disappeared with existing cache intact after recognizing a retained optimistic source ID behind a projected display ID. The native transcript and direct Gateway history each contain one terminal reply. iOS simulated-free archive reading and single native export passed. Later archive/version paywall copy changes have 67 targeted tests and still require fresh full gates/native builds.

- 09:30 candidate: composer persistence now isolates identical Agent/session names across connections; legacy unowned drafts are recoverable through an explicit preview in Add, with no automatic send or overwrite. Connection deletion clears only its owned v2 keys. Storage-read failure preserves unread data. Simulated-free override was turned off through iOS settings; both platforms returned to original system appearance/font settings. Later paywall/draft changes remain pending full gate and native validation. Generated Android app build output was moved to the task SSD cache via a symlink after repeated system-disk exhaustion; user data was not removed.

- 09:45 owner visual revision: rebuilt widget templates around the supplied YouMind chat capsule/circular-actions reference; added camera/photo shortcuts through existing attachment gates and late-result scope guards, with Android compact resizing. Session Panel creation moved out of the crowded header into a quiet leading list row, search became a stable body field, and the fixed detent increased 93→95%. Targeted 81 tests passed; native builds, full required gate and actual screenshots are in progress. No visual acceptance is claimed yet.

- 10:18: required 312 passed (322 Mobile suites / 3,308 tests). iOS 315 widget light/dark small+medium screenshots reviewed; small voice Link opened explicit recording controls, medium photo Link opened the native library, and explicit test-image send to Hermes returned the correct flower colors (native messages 185/186). Session Panel normal-size screenshot confirms quiet creation/search and higher detent. Actual large-text testing exposed compact header/search clipping and stale row measurements on hot font changes; Android host rejected the unsupported Space layout element. Both corrections are being rebuilt and are not yet visually accepted. USB transport required ADB restart and bounded chunked screenshot transfer; phone data/pairings were preserved.

Current repository metrics (includes unrelated prior work): non-test TS/TSX 136,985 lines, tests 89,402 lines in 400 files, 82 Markdown documents. Non-test total exceeds the M0 baseline by 12,838 lines. The approved scope has grown to additional backend support, sharing/widgets, task workflows and reliability work; this is not a claim that the full difference belongs to this task. Native Swift/Kotlin/XML are outside this TS metric. Do not delete tests or compress source to reduce the count; future cleanup must be consumer-backed and preserve the 90-day compatibility boundary.

- 10:40: required 327 passed (322 Mobile suites / 3,308 tests). iOS 326 passed cold/hot accessibility-size Session Panel checks and real leading-row session creation (331); normal size restored. Android 325 installed in place at device 09:28:18; voice and skill shortcut assertions passed, but the 335 script ended during a USB-interrupted screenshot, so it is not a whole-flow pass. The owner rejected Android widget quality again. Candidate 337 now consumes exact launcher sizes and scales capsule/circles/avatar/insets together; built and installed at device 09:38:16. Actual screenshot 339 was reviewed and shared; compact/night/shortcut acceptance continues.

- 10:56: Android 345 / iOS 345 Release built; Android installed at device 09:48:16, iOS installed. Required 348 passed (322 Mobile suites / 3,310 tests); prior 345 failed because two explicit native test mocks omitted BackHandler, corrected without weakening assertions. Android final widget geometry/mark was reviewed in small+wide/light+dark. Hardware Back now closes the visible shared sheet, verified from voice → retained chat. Session creation/reopen/Back assertions all passed in 351, although driver teardown lost USB. Native photo selection → explicit send → ANDROID_WIDGET_IMAGE_OK passed whole flow 357; native messages 187/188 confirm correct logo recognition. Camera permission → actual Samsung Camera → cancel returned without capture. Later visual inspection exposed square elevation shadows after hot theme changes in chat chrome; investigation continues.

- 11:14: Android 366 and iOS 372 Release built/installed. Exact circular radii and explicit light border reset corrected the Android hot-theme square shadows; same mounted chat was captured before/during/after theme changes. Android 130% font Session Panel and iOS 374 normal-size final panel were reviewed, then Android font restored. iOS 374 whole flow passed. Required 371 exposed two stale radius assertions, updated; 375 included the newly added failing draft regression, so neither run is a green gate. A controlled late-write regression reproduced submitted text returning to storage; drafts now serialize reads/writes/removal and atomically compare/consume legacy copies relative to App writes. Targeted 21 draft tests pass; final native builds/full gate pending. Actual widget screenshot evidence is in `evidence/workflows-2026-09-21/widget-review/`; owner visual acceptance and the remaining workflow scope are still outstanding.

- 11:39: required 378 and 394 passed (Mobile 322 suites / 3,314 tests); Release Android 395 and iOS 394 built/installed with image thumbnails in the native-share destination sheet, verified on both devices. Android Samsung Gallery → share → Hermes → explicit image send passed whole flow 393; iOS Photos → share → draft visually confirmed, then explicit send passed 397. Initial iOS image-node assertion was not a functional failure; the screenshot showed the adopted image. Direct ADB SEND failed to read its content URI, while actual Gallery-granted sharing succeeded. Native replies ANDROID_SYSTEM_IMAGE_OK / IOS_SYSTEM_IMAGE_OK confirm content recognition.
- Cross-device history exposed duplicate Hermes image user rows: the local text omitted native `[screenshot]` markers. Candidate Bridge retains an in-memory image count and projects only known image sends for exact one-to-one matching, keeping clean local text and native IDs. Targeted 31 tests passed; required 402 passed with the later Bridge change and v1 replay 403 passed 39 tests. Local Bridge rebuild/native cross-device verification is in progress. No external Hermes source, native database or production Worker was modified.

- 11:44: local CLI 405 rebuilt and managed runtimes restarted (406). Android 407 image send/reply assertions passed; screenshot/driver teardown hit USB loss, so not a complete script pass. Separate ADB capture 408 confirms the reply. Authenticated read-only live Bridge history 409 returned exactly one IMAGE_RECONCILE_407 user row, a native message ID, clean text and no active run; iOS whole flow 409 displayed the same single user row/reply from the other device. Final installed widget screenshot 410 reviewed and saved beside the light/dark/compact evidence. Phone left on the widget page with normal font/light appearance. Docs and whitespace checks pass; no later runtime changes after required 402. Broader W5–W8 incomplete scope remains as recorded above.

### On-demand file retrieval (2026-09-21 owner-approved candidate)

The chat Add sheet offers Session files only when the active Bridge advertises support. Listing reads the latest 200 native history messages and recognizes explicit assistant Markdown links, MEDIA paths and inline code paths. Only allowed regular files up to 10 MiB inside the native OpenClaw agent workspace, or Hermes configured local terminal cwd/outputs, are offered. This is not a computer file browser; unsupported types, private paths, symlinks and hardlinks are omitted.

The phone requests 128 KiB chunks only after a tap, verifies metadata/offset/length/completion, then opens the system share/save sheet. Cancellation removes partial local data; iOS completed temporary files are removed after sharing. Android retains only a local cache copy for the receiving app to read its granted URI; the chooser returning is not proof the receiver has finished. Abandoned feature-owned cache directories expire after 24 hours and are cleaned on the next download. File handles live in Bridge memory for 15 minutes and are session-scoped. Modified/replaced source files fail closed. Existing authenticated transport carries bytes; no new cloud file store, cloud queue, or cloud content logging is introduced. Relay forwarding is not a promise that bytes never transit the server, nor an additional end-to-end encryption claim.

Remote push remains deferred. A future generic task-completed push need not contain chat text or file contents, but requires device routing metadata and platform push infrastructure; tunnel-only connectivity does not guarantee background wake-up.

### Imported input presentation correction

Claude CLI may import an expanded copy of a mobile request after the original user row. Known explicit-skill prefixes and media/file extraction envelopes are decoded only on CLI-provenance rows. Deduplication requires adjacent native rows, the original mobile send key, an exact recovered request, and a forward timestamp within 60 seconds. This is read-only presentation; native model input and stored transcripts remain unchanged. Document draft cards center their icon/name vertically while retaining a growing two-line filename layout.

### 2026-09-22 — 技能详情进入聊天

技能详情底部固定「使用技能」入口，关闭弹窗后进入同一连接、同一 Agent 的主会话。复用聊天技能选择的适配器 invocation 和可用性规则；未启用、缺少依赖或被限制的技能按钮禁用。等待会话草稿恢复后选中技能，保留原有正文并替换旧技能标签，不自动发送。用户要求不运行测试，真机验收由用户完成。

### Agent profile task counts (2026-09-22)

The profile Cron card and job list share `loadAgentCronJobs`: paginate the connection-wide list, filter by Agent ownership, then count the returned jobs (including disabled and system-owned tasks). Unassigned legacy jobs belong to the default Agent under the existing compatibility policy. Failure notifications use that same filtered set across pages. A new Agent with no tasks displays zero; OpenClaw may automatically create its own maintenance task, which counts only for its owner. Global scheduler totals are not Agent totals.

### Create Agent entry lifecycle (2026-09-22)

The roster creation action is a one-shot navigation intent: clear `action=create-agent` when the focused identity page handles it. Reconnecting may unmount/remount the identity content, but must not replay creation. Leaving the page closes its creation sheet; purchase continuations may open it only while the original adapter/Agent scope and page remain active. A new explicit creation action can open the form again.
